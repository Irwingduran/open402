{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @open402/db run generate && pnpm -r --filter='./packages/*' run build && pnpm --filter @open402/web run build",
  "outputDirectory": "apps/web/.next"
}
